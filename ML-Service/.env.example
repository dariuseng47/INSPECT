# คัดลอกเป็น .env แล้วกรอกค่าจริงของเครื่องนี้ (.env ไม่ถูก commit เข้า git)

# ===== ML-Service server access (SFTP/SSH) =====
ML_SERVICE_HOST=
ML_SERVICE_PORT=22
ML_SERVICE_USER=
ML_SERVICE_PASSWORD=
